name: guia_de_moteis_clone
description: "Teste para Desenvolvedor Mobile (Flutter) - Guia de Motéis"
publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.6.2

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_bloc: ^9.0.0
  equatable: ^2.0.7
  http: ^1.3.0
  auto_size_text: ^3.0.0
  carousel_slider: ^5.0.0
  get_it: ^8.0.3
  cached_network_image: ^3.4.1
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
